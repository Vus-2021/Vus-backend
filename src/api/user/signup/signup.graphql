scalar Date

enum Group {
    VT
    VH
    ES
    RY
    WR
}

input UserPayment {
    userId: ID!
    password: String!
    name: String!
    phoneNumber: String!
    group: Group!
    registerDate: Date
}

type SignupResponse implements Response {
    success: Boolean!
    message: String!
}

type Mutation {
    signupUser(input: UserPayment): SignupResponse
}

type Query {
    dummy: String
}
