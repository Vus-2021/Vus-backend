type UserPayload {
    name: String
    userId: ID
    type: UserType
}

type getMyInformationResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
    data: UserPayload
}

type Query {
    getMyInformation: getMyInformationResponse
}
