type Query {
    getDriverNotice(route: RouteNames): GetDriverNoticeResponse
}

type GetDriverNoticeResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
    data: driverNotice
}

type driverNotice implements Notice {
    route: String
    delayTime: String
    noticeType: NoticeTypes
    notice: String
    createdAt: String
}

interface Notice {
    notice: String
    createdAt: String
}
