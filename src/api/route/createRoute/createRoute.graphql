enum RouteNames {
    ANSAN
    GANGNAM
    SUNGNAM
    BYENGJUM
    MANGPO
}

type createRouteResponse implements Response {
    success: Boolean!
    message: String!
    data: String
}

input driver {
    name: String
    image: String
    phone: String
}

type Mutation {
    createRoute(
        routeName: RouteNames
        createdAt: String
        busNumber: String
        limitCount: Int
        driver: driver
    ): createRouteResponse
}
