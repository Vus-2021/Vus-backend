type Query {
    getRoutesInfo(month: String): RouteInfoResponse
}

type RouteInfoResponse implements Response {
    success: Boolean!
    message: String!
    cod: Int
    data: [RouteInfo]
}

type RouteInfo {
    route: String
    busNumber: String
    limitCount: Int
    registerCount: Int
    driver: driverInfo
}

type driverInfo {
    phone: String
    image: String
    name: String
}
