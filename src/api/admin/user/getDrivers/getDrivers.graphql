type Query {
    getDrivers: GetDriversResponse
}

type GetDriversResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
    data: [Driver]
}

type Driver {
    userId: String
    name: String
    type: String
    registerDate: Date
    phoneNumber: String
}
