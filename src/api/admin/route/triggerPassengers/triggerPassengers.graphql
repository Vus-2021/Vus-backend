type Mutation {
    triggerPassengers(month: String!, route: String!, busId: String!): triggerPassengersResponse
    #triggerPassengersResponse(month: String!, route: String!, busId: String!, methodList: [String], sortType: String)
}

type triggerPassengersResponse implements Response {
    success: Boolean!
    message: String!
    code: Int!
}
