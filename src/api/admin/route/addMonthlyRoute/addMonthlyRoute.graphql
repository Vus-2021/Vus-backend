type Mutation {
    addMonthlyRoute(partitionKey: String!, month: String!): addMonthlyRouteResponse
}

type addMonthlyRouteResponse implements Response {
    success: Boolean!
    message: String!
    code: Int
}
